(a) MISSING / CONTRADICTORY

1. **Three incompatible analytics vocabularies.** ANALYTICS declares a closed param set (`language, service_slug, turn_count, qualification_step, question_category`) and events `chat_question`/`chat_whatsapp_transfer`/etc. FLOWS Â§2.7 fires `chat_whatsapp_transfer` with `{service, locale, qualified, trigger}` â€” none of those params exist in the ANALYTICS schema. PRIVACY Â§1.3 lists events that exist nowhere else (`chat_first_message`, `chat_message_sent`, `chat_handover_whatsapp`) and PRIVACY Â§3.1/Â§3.2 fire `chat_error`/`chat_rate_limited`, which the ANALYTICS plan never defines (no params, no dashboard row â€” failure modes are unmeasured).
2. **WhatsApp transfer trigger mismatch.** ANALYTICS says `chat_whatsapp_transfer` fires on the anchor carrying the *generic* prefill (`?text=Hello%20I%20am%20interested...`) and debates `data-track` delegation; FLOWS Â§2.4 builds a *custom* `?text=` payload from the approved summary. The ANALYTICS test method would fail against the FLOWS implementation.
3. **Handover violates the privacy spec as written.** PRIVACY Â§1.2: "message content never appears in query strings." FLOWS Â§2.4 URL-encodes a visitor-derived summary into `wa.me/...?text=`. Needs an explicit documented exception (user-approved, user-initiated, PII-stripped, third-party deep link) or the handover is forbidden by its own privacy section.
4. **Emergency policy contradiction.** KB G11: "direct the person to their local emergency number." PRIVACY Â§1.4: "never to emergency services routing â€¦ it simply states it is not for emergencies." FLOWS routes MED â†’ WhatsApp handover unconditionally, while KB's escalation table says emergencies get no sales content.
5. **Three "canonical" availability wordings.** PRIVACY Â§4.1 declares its business-hours line "the only permitted handover wording in v1"; FLOWS Â§2.6 declares a different "standard fallback, all locales"; FLOWS microcopy offline message is a third. Worse, Â§4.1's "wir antworten wÃ¤hrend unserer GeschÃ¤ftszeiten" asserts business hours exist and govern replies â€” hours are unconfirmed; FLOWS' wording is the safe one.
6. **Per-page prompt variants break the caching design.** PRIVACY Â§1.4 says healthcare pages get their own enforcing system prompt; KB Â§4's economics and hash-integrity check assume one byte-stable cached prefix. Pick: single prompt + UI-only disclaimer, or redo the cache/hash story.
7. **No mechanism produces the analytics enums.** ANALYTICS needs per-turn `question_category`, `service_slug` resolution, `chat_pricing_view` ("price card" â€” FLOWS defines no cards), and a `qualification_step` state machine; PRIVACY Â§2.4 Layer 2 says the model has no tools/function calling. No draft says how enums are emitted (structured output? second classifier call? client regex?).
8. **Qualification data is orphaned.** FLOWS Q1â€“Q3 (business type / service / goal) produce only a `qualified` boolean that ANALYTICS doesn't accept; ANALYTICS' `qualification_step` enum is a different concept; Q1/Q3 answers are collected and never used by anything.
9. **Undefined failure UX.** PRIVACY Â§2.3's 20-turn cap has no user-facing message in FLOWS or Â§3; KB Â§1.3's "refuses to boot on hash mismatch" has no visitor-facing behavior (presumably the 503/`chat_disabled` path â€” unstated); PRIVACY Â§2.6's output filter has no fallback when a reply is heavily sanitized.
10. **Refusal template not followed by the examples.** KB Â§3.2 mandates verbatim structure with all three channels; FLOWS 3a/3b use free-form refusals offering only WhatsApp. Decide template-verbatim vs template-as-pattern.
11. **"excl. VAT" is not in the KB pipeline's sources.** G3 requires reciting "excl. VAT" with every price, but the VAT note lives in `src/i18n/ui.ts` (`price.vatNote`), while the build script imports only `pricing.ts` + `site.ts`. As specced, the mandated disclosure isn't in the generated KB.

(b) INVENTED FACTS â€” verified against `C:\Users\giova\OneDrive\Desktop\weissmann-ai\src\data\pricing.ts`, `site.ts`, `ConsentAnalytics.astro`, `i18n/ui.ts`:
- **No invented prices.** Every figure in the examples matches source: 350/1500 min/CHF 0.30; 990/3500/CHF 0.25; Enterprise null; 2'490; 4'990; from 9'900; SEO 890; GEO 990; Stripe links exist for exactly `phone-starter` and `phone-premium`; disclosures, features (voice cloning, smart routing, dedicated line + hosting), phone, email, address, GA4 `G-3L30SCGWGT`, `wm-consent`, `wmEvent`, `a[data-track]` delegation, generic WhatsApp prefill, `/kontakt/` locale routes â€” all real.
- **One violation:** PRIVACY Â§4.1's mandated wording "we reply during business hours" asserts unconfirmed business hours in all four languages â€” the same class of claim Â§4.1 itself calls a HARD-LIMIT violation. Drop it for the FLOWS Â§2.6 wording.
- Borderline: PRIVACY Â§1.5's "Anthropic API input/output is not used for training by default" is stated as fact â€” keep it hedged/lawyer-verify only (it is flagged, acceptable).

(c) TOP 5 FIXES
1. Adopt the ANALYTICS event/param schema as the single source: rewrite FLOWS Â§2.7's payload to `{language, service_slug, turn_count, qualification_step}`, replace PRIVACY Â§1.3's event list, and add `chat_error` + `chat_rate_limited` rows (with an `error_code` enum param) to the plan and dashboard.
2. Unify emergency handling across KB G11 / PRIVACY Â§1.4 / FLOWS MED branch (recommend: state not-an-emergency-service + refer to local emergency number, no WhatsApp CTA in that message), and align the three drafts verbatim.
3. Make FLOWS Â§2.6 the single availability fallback wording; delete PRIVACY Â§4.1's business-hours line (it violates the unconfirmed-hours hard limit) and reuse Â§2.6 wording in offline/rate-limit copy.
4. Add an explicit wa.me exception to PRIVACY Â§1.2 (user-approved, user-initiated, PII-stripped prefill only) and fix the ANALYTICS `chat_whatsapp_transfer` trigger/test to reference the custom-built summary link, fired manually (no `data-track`).
5. Specify the enum-production mechanism (recommend: server post-processes each model reply into `{question_category, service_slug, price_quoted}` via structured output; client forwards enums only), define turn-cap and sanitized-reply fallback messages in FLOWS/failure sections, and add `price.vatNote` from `i18n/ui.ts` to the generated KB so G3 is groundable.

Deliverable coverage 4â€“12: all present (6 includes all four languages); the gaps above are consistency, not absence.
